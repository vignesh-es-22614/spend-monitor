# Never commit credentials
*.key.json
service-account*.json
bigquery_token.json
# GCP downloads keys as <project>-<hash>.json — belt and braces, though the
# key should live outside the repo entirely (prepare-deploy.js enforces that).
it-security-online-marketing-*.json
*-????????.json
.env
.env.*
!.env.deploy.example

# Generated by scripts/prepare-deploy.js — contains the service account key.
# Only the .template.json files are committed.
functions/*/catalyst-config.json

# Catalyst
.catalystrc
.catalyst/
node_modules/

# Local dry-run output
preview.html
preview.txt
data.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
